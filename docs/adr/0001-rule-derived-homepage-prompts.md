# Generate Homepage Action Prompts from Explicit Rules

The homepage will select action-prompt candidates from real personal-workbench facts using deterministic, inspectable rules; LLM generation and model-based ranking are outside the initial contract. This preserves reproducibility and lets each prompt explain its source, reason, and real processing action.
