# Show Real Menstrual Details on the Homepage by Default

The homepage will keep the menstrual source enabled by default and show real menstrual details when the source provides them, matching the current code and the user's chosen all-sources display policy. Missing, unreliable, or absent prediction fields still produce an explicit empty state rather than an inferred health conclusion; revisit this decision before adding new sensitive sources or sharing the workspace with other users.
